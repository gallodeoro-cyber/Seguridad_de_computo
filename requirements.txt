# Dependencias directas, versiones usadas para esta entrega.
Flask==3.1.3
Flask-WTF==1.3.0
email-validator==2.3.0
python-dotenv==1.2.3
# Render usa Linux. Gunicorn no se ejecuta en Windows.
gunicorn==26.2.0; sys_platform != "win32"
